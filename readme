So I've made a display dashboard for a computer monitor, I have made the frontend myself in HTML & CSS.
I need your help in implementing the functionality:

.Time and date to match the system
.Get current weather of specified location
    .Display the current tempreture
    .Display the current weather state (in text)
    .Display the spcified loaction
    .Display an icon of the current weather state
.Get the weather forcast for the next three days
    .display the days in three letter characters (ddd)
    .display an icon for the weather of that days
    .display the tempreture of that days
.calendar
    .access google calendar
    .display the date of each entry and the day full name (dddd)
    .display the time of each event
    .display the name/subject of each event
    .display the location of each event (if location specified)
.Settings
    .open-settings button to make visible the moodle-settings
    .moodle-close-button to make the moodle not visible
    .location-input value to be the location used to in accessing the weather api
    .weather-api-input to be used in providing the weather api used in accessing the weather api
